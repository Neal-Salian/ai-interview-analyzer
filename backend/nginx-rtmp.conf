rtmp {
    server {
        listen 1935;
        chunk_size 4096;

        application live {
            live on;
            record off;

            # Push stream to your FastAPI processor
            exec ffmpeg -i rtmp://localhost/live/$name
                -f rawvideo -pix_fmt bgr24 pipe:1;
        }
    }
}